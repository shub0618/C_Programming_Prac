#include <stdio.h> 
int main () {
 
  // Declaring four local variables
  int p, q, r;
  int s;
 
  // Initializing the variables
  p = 2;
  q = 5;
  r = 7;
  s = p * q * r;
 
  // Display the variables
  printf ("p = %d",p);
  printf ("\nq = %d",q);
  printf ("\nr = %d",r);
  printf ("\ns (Multiplication Result) = %d",s);
 
  return 0;
}